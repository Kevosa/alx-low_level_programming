#include "main.h"
/**
 * main - print alphabet in small case
 *
 * Return: 0
 */
void print_alphabet(void)
{
	char letter;
	if (letter = a ;letter <= z ;letter++)
	{
		_putchar(letter);
	}
		_putchar('\n');
	return(0);
}

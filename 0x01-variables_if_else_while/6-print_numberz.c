#include<stdio.h>
/**
 * main - print all single digits of base 10
 *
 * Return: alaways 0
 */
int main(void)
{
	int digit;

		for (digit = 0 ; digit < 10 ; digit++)
		{
			putchar(digit);
		}
	putchar('\n')
		return (0);
}

#include<stdio.h>
/**
 * main - print all single digit number to base 10
 *
 * Return:alaways0
 */
int main(void)
{
	int digit;

	for (digit = 0 ; digit < 10 ; digit++)
	{
		printf("%d", digit);
	}
	printf("\n");
		return (0);
}
